<ContentDialog
    x:Class="PlanNotes.View.Dialog.AddCheckListDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlanNotes.View.Dialog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    BorderThickness="1"
    CornerRadius="4"
    DataContext="{Binding Source={StaticResource VML}, Path=AddCheckListDialog}"
    BorderBrush="{StaticResource Color3}"
    x:Name="AddCheckList">

    <ContentDialog.Resources>
        <Thickness x:Key="ContentDialogPadding">32</Thickness>
    </ContentDialog.Resources>

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock
            Text="Add CheckList"
            FontSize="20"
            FontWeight="SemiBold"
            TextAlignment="Center" />
        <TextBox
            Grid.Row="1"
            Width="264"
            Text="{Binding NameCheckList, Mode=TwoWay}"
            Header="Name (Required)"
            PlaceholderText="Enter name of checklist"
            BorderThickness="1"
            BorderBrush="{StaticResource Color3}" />
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button
                Content="Add"
                Command="{Binding AddCheckListCommand}"
                HorizontalAlignment="Stretch"
                Background="{StaticResource Color6}"
                CornerRadius="4"
                BorderThickness="1"
                BorderBrush="{StaticResource Color3}">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{StaticResource Color6}" />
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{StaticResource Color6}" />
                </Button.Resources>
            </Button>
            <Button
                Content="Cancel"
                Grid.Column="1"
                Command="{Binding HideDialogCommand}"
                HorizontalAlignment="Stretch"
                CornerRadius="4"
                BorderThickness="1"
                BorderBrush="{StaticResource Color3}" />
        </Grid>
    </Grid>
</ContentDialog>
